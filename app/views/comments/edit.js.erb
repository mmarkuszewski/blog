//pomienia tresc komentarza na jego forme pozwalającą go edytować
$('a').bind('ajax:complete', function () {
    if($("div#"+this.id).length)
    {
        $("div#" + this.id)[0].innerHTML = "<%= escape_javascript(render('comment_form')) %>";
    }
});